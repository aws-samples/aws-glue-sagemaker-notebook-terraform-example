# This is a Home Brew bundle file. To install all dependencies, run brew bundle
# Since terraform is installed by tfenv, it's not listed here.

brew "tfenv"
brew "pre-commit"
brew "tfsec"
brew "tflint"
brew "terraform-docs"